
project('EAWebKit_Full')

# Just pass it on to EAWebKit, this is the main project because
# the support packages are in sibling directories which meson doesn't like.
EAWebKit = subproject('EAWebKit')

